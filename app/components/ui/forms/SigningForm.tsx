"use client";

import Link from "next/link";


export function SigningForm() {
    return (
        <div>SigIn</div>
        /*<Card className="shadow-lg">
            <Card.Body className="p-5">
                <Card.Title className="fs-4 fw-bold mb-4">Login</Card.Title>
                <Form>
                    <Form.Group className="mb-3">
                        <Form.Label className="mb-2 text-muted" htmlFor="email">Email</Form.Label>
                        <Form.Control
                            required
                            id="identifier"
                            name="identifier"
                            type="text"
                            placeholder="username or email"
                            autoFocus
                        />
                    </Form.Group>
                    <Form.Group className="mb-3">
                        <Form.Label className="text-muted" htmlFor="password">Password</Form.Label>
                        <Form.Control
                            required
                            id="password"
                            name="password"
                            type="password"
                            placeholder="password"
                        />
                    </Form.Group>
                    <div className="d-flex align-items-center">
                        <Button className="ms-auto" variant="primary" type="submit">Sign In</Button>
                    </div>
                </Form>
            </Card.Body>
            <Card.Footer className="py-3 border-0">
                <div className="text-center">
                    Don't have an account? <Link href={"/auth/signup"} className="text-dark">Sign Up</Link>
                </div>
            </Card.Footer>
        </Card>*/
    );
}