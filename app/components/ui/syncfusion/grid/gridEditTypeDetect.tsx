import React from 'react';
import {DynamicColumn} from "@/app/components/model/dynamicColumn";

const GridEditTypeDetect = (column: DynamicColumn) => {
    return (
        <div>

        </div>
    );
};

export default GridEditTypeDetect;